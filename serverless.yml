service: basic-serverless-lambda

provider:
  name: aws
  # runtime: nodejs12.x
  runtime: lambda-php-74
  stage: stage
  region: us-east-2

functions:
  hello:
    # handler: src/handler.generateRandomNumber
    handler: public/index.php
    timeout: 60 # seconds
    memorySize: 128 # mb
